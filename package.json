{
  "name": "aiva-enterprises",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix admin && npm install --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix admin && mkdir -p dist && cp -r frontend/dist/* dist/ && mkdir -p dist/admin && cp -r admin/dist/* dist/admin/"
  }
}
